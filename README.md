# marcus.wagner.github.io
This is the repository for Marcus Wagner's portfolio, built with React + Vite.

## Local development

```
npm install
npm run dev
```

## Deployment

Deploys happen automatically via GitHub Actions (`.github/workflows/deploy.yml`) on every push to `main`. In the repo's Settings → Pages, the source must be set to "GitHub Actions".
